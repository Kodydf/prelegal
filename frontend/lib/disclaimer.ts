// The draft / legal-review wording, in one place so the screens, the document and the PDF can't drift.

/** Shown at the top of every generated document, on screen and in the PDF. */
export const DRAFT_NOTICE =
  "DRAFT: This document is a draft and is subject to legal review before it is signed or relied on.";

/** Repeated at the bottom of every page of the PDF. Keep it static text (see DocumentPdf). */
export const PDF_FOOTER = "Draft, subject to legal review";

/** A reminder next to the download button. */
export const DRAFT_REMINDER = "This is a draft and is subject to legal review before it is signed or relied on.";

/** The app-wide statement (footer, sign-in screen). */
export const SITE_DISCLAIMER =
  "Prelegal provides drafting assistance, not legal advice. Documents are drafts and are subject to legal review.";
